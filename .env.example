# Copy to .env and fill in. Get a key at https://typesafe.ai
TYPESAFE_API_KEY=apikey_...

# Optional overrides — the defaults below are what the SDK uses anyway.
# TYPESAFE_BASE_URL=https://api.typesafe.ai
# TYPESAFE_DEFAULT_MODEL=jev-latest
# TYPESAFE_LOG_LEVEL=INFO
